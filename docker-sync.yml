version: "2"

options:
  verbose: true
syncs:
  ${PROJECT_NAME}-app-sync:
    src: '${SOURCE_PATH}'
    sync_userid: '1001'
    sync_excludes: ['var/cache','var/session']
    watch_excludes: ['var/cache','var/session']